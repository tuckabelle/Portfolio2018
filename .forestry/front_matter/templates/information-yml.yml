---
label: Contact Information
hide_body: true
fields:
- name: contact_info
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
  - name: url
    type: text
    config:
      required: false
    label: URL
  - name: icon
    type: file
    config:
      maxSize: 250
    label: Icon
  config:
    min: 
    max: 
    labelField: 
  label: Contact Info
pages:
- _data/information.yml
